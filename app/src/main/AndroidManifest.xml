<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.travel_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Travel_App">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".navigation.NavActivity"
            android:exported="false"
            android:label="NavActivity"
            android:theme="@style/Theme.Travel_App.NoActionBar" />
        <activity android:name=".navigation.ui.home.AddTripActivity" />
        <activity android:name=".navigation.edit_trip.EditTripActivity" />
        <activity android:name=".navigation.ui.home.SortActivity"/>
    </application>

</manifest>